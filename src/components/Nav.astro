---
const menu = [
  {
    label: 'Online Casinos USA',
    href: '/online-casinos/',
    mega: true,
    groups: [
      {
        title: 'Browse Casinos', href: '/',
        links: [
          { label: 'Real Money Casinos', href: '/real-money-casinos/' },
          { label: 'Instant Play Casinos', href: '/desktop-instant-play-casinos/' },
          { label: 'High Roller Casinos', href: '/high-roller-casinos/' },
          { label: 'Best Payout Casinos', href: '/best-payout-online-casinos/' },
          { label: 'Big Win Casinos', href: '/big-win-casinos/' },
          { label: 'New Online Casinos', href: '/new-casinos/' },
          { label: 'Download Casinos', href: '/desktop-download-casinos/' },
          { label: 'Sports Betting', href: '/sports-betting/' },
        ],
      },
      {
        title: 'Payment Methods', href: '/casino-banking/',
        links: [
          { label: 'PayPal Casinos', href: '/casino-banking/paypal/' },
          { label: 'Discover Casinos', href: '/casino-banking/discover/' },
          { label: 'Crypto Casinos', href: '/casino-banking/cryptocurrency/' },
          { label: 'ACH Casinos', href: '/casino-banking/ach/' },
          { label: 'Tether Casinos', href: '/casino-banking/tether/' },
          { label: 'Instadebit Casinos', href: '/casino-banking/instadebit/' },
          { label: 'Neteller Casinos', href: '/casino-banking/neteller/' },
          { label: 'Ethereum Casinos', href: '/casino-banking/ethereum/' },
        ],
      },
      {
        title: 'Recommended Casinos', href: '/casino-reviews/',
        links: [
          { label: 'BetOnline Casino', href: '/casino-reviews/betonline-casino/' },
          { label: 'Café Casino', href: '/casino-reviews/cafe-casino/' },
          { label: 'Duckyluck Casino', href: '/casino-reviews/duckyluck-casino/' },
          { label: 'Lucky Tiger Casino', href: '/casino-reviews/lucky-tiger-casino/' },
          { label: 'Ignition Casino', href: '/casino-reviews/ignition-casino/' },
          { label: 'Slots of Vegas Casino', href: '/casino-reviews/slots-of-vegas-casino/' },
          { label: 'Slots.lv Casino', href: '/casino-reviews/slots-lv-casino/' },
          { label: 'Wild Casino', href: '/casino-reviews/wild-casino/' },
          { label: 'MyBookie Casino', href: '/casino-reviews/mybookie-casino/' },
        ],
      },
    ],
  },
  {
    label: 'Casino Bonuses',
    href: '/casino-bonuses/',
    mega: true,
    groups: [
      {
        title: 'Bonus Types', href: '/casino-bonuses/',
        links: [
          { label: 'Welcome Bonuses', href: '/casino-bonuses/welcome-bonus/' },
          { label: 'Free Spins Bonuses', href: '/casino-bonuses/free-spins/' },
          { label: 'Free Chip Bonuses', href: '/casino-bonuses/free-chips/' },
          { label: 'Birthday Bonuses', href: '/casino-bonuses/birthday/' },
          { label: 'Referral Bonuses', href: '/casino-bonuses/referral/' },
        ],
      },
      {
        title: 'No Deposit Bonuses', href: '/no-deposit/',
        links: [
          { label: '$20 No Deposit', href: '/no-deposit/20-dollar/' },
          { label: '$50 No Deposit', href: '/no-deposit/50-dollar/' },
          { label: '$100 No Deposit', href: '/no-deposit/100-dollar/' },
          { label: 'Ethereum No Deposit', href: '/no-deposit/ethereum/' },
        ],
      },
      {
        title: 'Minimum Deposit', href: '/minimum-deposit-casinos/',
        links: [
          { label: '$1 Minimum Deposit', href: '/minimum-deposit-casinos/1-dollar/' },
          { label: '$5 Minimum Deposit', href: '/minimum-deposit-casinos/5-dollar/' },
          { label: '$10 Minimum Deposit', href: '/minimum-deposit-casinos/10-dollar/' },
          { label: '$20 Minimum Deposit', href: '/minimum-deposit-casinos/20-dollar/' },
        ],
      },
    ],
  },
  {
    label: 'Slots Online',
    href: '/online-slots/',
    groups: [
      {
        title: 'Slots', href: '/online-slots/',
        links: [
          { label: 'Slots Apps', href: '/online-slots/apps/' },
          { label: '3D Slots', href: '/online-slots/3d/' },
          { label: 'Slot Reviews', href: '/online-slots/reviews/' },
        ],
      },
      {
        title: 'Slot Themes', href: '/online-slots/themes/',
        links: [
          { label: 'Superhero Slots', href: '/online-slots/superhero/' },
          { label: 'Battle Slots', href: '/online-slots/battle/' },
          { label: 'Christmas Slots', href: '/online-slots/christmas/' },
          { label: 'Mystery Slots', href: '/online-slots/mystery/' },
          { label: 'Jungle Slots', href: '/online-slots/jungle/' },
        ],
      },
    ],
  },
  {
    label: 'Casino Games',
    href: '/casino-games/',
    align: 'right',
    groups: [
      {
        title: null, href: null,
        links: [
          { label: 'Online Blackjack', href: '/online-blackjack/' },
          { label: 'Online Roulette', href: '/online-roulette/' },
          { label: 'Online Baccarat', href: '/online-baccarat/' },
          { label: 'Online Craps', href: '/online-craps/' },
          { label: 'Online Poker', href: '/online-poker/' },
          { label: 'Online Video Poker', href: '/online-video-poker/' },
        ],
      },
      {
        title: null, href: null,
        links: [
          { label: 'Online Sic Bo', href: '/online-sic-bo/' },
          { label: 'Online Keno', href: '/online-keno/' },
          { label: 'Online Bingo', href: '/bingo-online/' },
          { label: 'Online Scratch Cards', href: '/online-scratch-cards/' },
          { label: 'Online Lottery', href: '/online-lottery/' },
        ],
      },
    ],
  },
  {
    label: 'Mobile Casinos',
    href: '/mobile-casinos/',
    align: 'right',
    groups: [
      {
        title: null, href: null,
        links: [
          { label: 'iPhone Casinos', href: '/iphone-casinos/' },
          { label: 'Android Casinos', href: '/android-casinos/' },
          { label: 'iPad Casinos', href: '/mobile-casinos/ipad/' },
          { label: 'Mac Casinos', href: '/mac-casinos/' },
        ],
      },
      {
        title: null, href: null,
        links: [
          { label: 'Blackberry Casinos', href: '/mobile-casinos/blackberry/' },
          { label: 'Windows Casinos', href: '/mobile-casinos/windows/' },
          { label: 'Mobile Casino Bonuses', href: '/mobile-casinos/bonuses/' },
        ],
      },
    ],
  },
  { label: 'Blogs', href: '/news/' },
];

const chev = '<svg class="chev" viewBox="0 0 12 12" width="11" height="11" aria-hidden="true"><path d="M2.5 4.5L6 8l3.5-3.5" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/></svg>';
---

<header class="nav" id="site-nav">
  <div class="wrap nav__inner">
    <a class="brand" href="/" aria-label="US Player Check home">
      <img class="brand__logo" src="/images/usplayercheck-blue_logo.png" alt="US Player Check" width="1944" height="221" />
    </a>

    <nav class="nav__links" aria-label="Primary">
      <ul class="megabar">
        {menu.map((item) => (
          <li class={`navitem ${item.groups ? 'has-panel' : ''} ${item.align === 'right' ? 'align-right' : ''}`}>
            <a class="navlink" href={item.href}>
              {item.label}
              {item.groups && <span class="navlink__chev" set:html={chev} />}
            </a>
            {item.groups && (
              <div class={`panel ${item.mega ? 'panel--mega' : ''}`} style={`--cols:${item.groups.length}`}>
                <div class="panel__grid">
                  {item.groups.map((g) => (
                    <div class="col">
                      {g.title && (
                        <a class="col__title" href={g.href || '#'}>{g.title}</a>
                      )}
                      <ul class="col__links">
                        {g.links.map((l) => (
                          <li><a href={l.href}>{l.label}</a></li>
                        ))}
                      </ul>
                    </div>
                  ))}
                </div>
              </div>
            )}
          </li>
        ))}
      </ul>
    </nav>

    <div class="nav__actions">
      <span class="nav__age" title="Players must be 18 or over">18+</span>
      <a class="btn btn-emerald nav__cta" href="/#casinos">See top picks</a>
    </div>

    <button class="nav__burger" aria-label="Open menu" aria-expanded="false" id="nav-burger">
      <span></span><span></span><span></span>
    </button>
  </div>

  <!-- Mobile menu -->
  <nav class="nav__mobile" id="nav-mobile" aria-label="Mobile">
    <div class="nav__mobile-inner">
      {menu.map((item) => (
        item.groups ? (
          <details class="m-item">
            <summary>
              <span>{item.label}</span>
              <span class="m-chev" set:html={chev} />
            </summary>
            <div class="m-panel">
              {item.groups.map((g) => (
                <div class="m-col">
                  {g.title && <a class="m-col__title" href={g.href || '#'}>{g.title}</a>}
                  {g.links.map((l) => <a class="m-link" href={l.href}>{l.label}</a>)}
                </div>
              ))}
            </div>
          </details>
        ) : (
          <a class="m-top-link" href={item.href}>{item.label}</a>
        )
      ))}
      <div class="nav__mobile-actions">
        <span class="nav__age">18+</span>
        <a class="btn btn-emerald" href="#casinos">See top picks</a>
      </div>
    </div>
  </nav>
</header>

<style>
  .nav {
    position: sticky;
    top: 0;
    z-index: 50;
    font-family: var(--body);
    background: color-mix(in srgb, var(--ivory) 88%, transparent);
    backdrop-filter: saturate(140%) blur(10px);
    -webkit-backdrop-filter: saturate(140%) blur(10px);
    border-bottom: 1px solid transparent;
    transition: border-color .3s ease, box-shadow .3s ease, background .3s ease;
  }
  .nav.is-stuck { border-bottom-color: var(--line); box-shadow: var(--shadow-sm); }

  .nav__inner { display: flex; align-items: center; gap: 118px; height: 72px; }

  .brand { display: inline-flex; align-items: center; gap: 10px; margin-right: auto; }
  .brand__mark { color: var(--gold); display: inline-flex; }
  .brand__logo { height: 34px; width: auto; display: block; }

  /* ---- Mega bar ---- */
  .megabar { list-style: none; margin: 0; padding: 0; display: flex; align-items: center; gap: 4px; }
  .navitem { position: relative; }
  .navlink {
    display: inline-flex; align-items: center; gap: 5px;
    font-size: 14.5px; font-weight: 600; color: var(--muted);
    padding: 10px 12px; border-radius: 10px;
    transition: color .16s ease, background .16s ease;
    white-space: nowrap;
  }
  .navlink:hover { color: var(--ink); background: var(--ivory-2); }
  .navlink__chev { display: inline-flex; color: var(--emerald); }
  .navlink__chev :global(svg) { width: 13px; height: 13px; transition: transform .2s ease; }
  .navitem:hover .navlink__chev :global(svg),
  .navitem:focus-within .navlink__chev :global(svg) { transform: rotate(180deg); }

  /* ---- Dropdown panel ---- */
  .panel {
    position: absolute;
    top: 100%;
    left: 0;
    margin-top: 10px;
    background: var(--white);
    border: 1px solid var(--line);
    border-radius: 16px;
    box-shadow: var(--shadow-lg);
    padding: 18px;
    opacity: 0;
    visibility: hidden;
    transform: translateY(8px);
    transition: opacity .2s ease, transform .2s ease, visibility .2s;
    z-index: 60;
  }
  .panel::before { /* hover bridge so the gap doesn't close the menu */
    content: ""; position: absolute; top: -12px; left: 0; right: 0; height: 12px;
  }
  .navitem:hover > .panel,
  .navitem:focus-within > .panel,
  .navitem.is-open > .panel { opacity: 1; visibility: visible; transform: translateY(0); }
  .navitem.is-open .navlink__chev :global(svg) { transform: rotate(180deg); }
  .navitem.align-right > .panel { left: auto; right: 0; }

  .panel__grid {
    display: grid;
    grid-template-columns: repeat(var(--cols), minmax(168px, 1fr));
    gap: 8px 22px;
  }
  .panel--mega .panel__grid { gap: 8px 30px; }

  .col { min-width: 0; }
  .col__title {
    display: block;
    font-size: 12px; font-weight: 700; letter-spacing: .05em; text-transform: uppercase;
    color: var(--gold-deep);
    padding: 6px 10px 8px;
    margin-bottom: 4px;
    border-bottom: 1px solid var(--line);
  }
  .col__title:hover { color: var(--emerald); }
  .col__links { list-style: none; margin: 0; padding: 0; display: grid; gap: 1px; }
  .col__links a {
    display: block;
    padding: 8px 10px;
    border-radius: 9px;
    font-size: 14px;
    color: var(--muted);
    transition: background .14s ease, color .14s ease, padding-left .14s ease;
  }
  .col__links a:hover {
    background: var(--ivory-2);
    color: var(--emerald);
    padding-left: 14px;
  }



  /* ---- Actions ---- */
  .nav__actions { display: flex; align-items: center; gap: 14px; }
  .nav__age {
    font-size: 12px; font-weight: 700; color: var(--emerald);
    border: 1.5px solid var(--emerald); border-radius: 999px; padding: 3px 8px; white-space: nowrap;
  }

  /* ---- Burger ---- */
  .nav__burger { display: none; flex-direction: column; gap: 5px; background: none; border: 0; padding: 8px; }
  .nav__burger span { width: 22px; height: 2px; background: var(--ink); border-radius: 2px; transition: transform .25s ease, opacity .2s ease; }
  .nav__burger[aria-expanded="true"] span:nth-child(1) { transform: translateY(7px) rotate(45deg); }
  .nav__burger[aria-expanded="true"] span:nth-child(2) { opacity: 0; }
  .nav__burger[aria-expanded="true"] span:nth-child(3) { transform: translateY(-7px) rotate(-45deg); }

  /* ---- Mobile menu ---- */
  .nav__mobile { display: none; border-bottom: 1px solid var(--line); background: var(--ivory); max-height: calc(100vh - 72px); overflow-y: auto; }
  .nav__mobile.is-open { display: block; }
  .nav__mobile-inner { padding: 8px var(--gutter) 24px; }

  .m-top-link, .m-item > summary {
    display: flex; align-items: center; justify-content: space-between;
    padding: 14px 4px; font-weight: 600; font-size: 16px; color: var(--ink);
    border-bottom: 1px solid var(--line); cursor: pointer; list-style: none;
  }
  .m-item > summary::-webkit-details-marker { display: none; }
  .m-chev :global(svg) { transition: transform .2s ease; color: var(--muted-2); }
  .m-item[open] > summary .m-chev :global(svg) { transform: rotate(180deg); }
  .m-item[open] > summary { color: var(--emerald); }

  .m-panel { padding: 8px 4px 14px; display: grid; gap: 14px; }
  .m-col { display: grid; gap: 2px; }
  .m-col__title { font-size: 12px; font-weight: 700; letter-spacing: .05em; text-transform: uppercase; color: var(--gold-deep); padding: 6px 8px; }
  .m-link { padding: 9px 8px; font-size: 14.5px; color: var(--muted); border-radius: 8px; }
  .m-link:active { background: var(--ivory-2); }

  .nav__mobile-actions { display: flex; align-items: center; gap: 14px; padding-top: 18px; }
  .nav__mobile-actions .btn { flex: 1; }

  /* ---- Breakpoint ---- */
  @media (max-width: 1040px) {
    .nav__links, .nav__cta, .nav__actions .nav__age { display: none; }
    .nav__actions { display: none; }
    .nav__burger { display: flex; }
  }
</style>

<script is:inline>
  (function () {
    var nav = document.getElementById('site-nav');
    var onScroll = function () { nav.classList.toggle('is-stuck', window.scrollY > 8); };
    onScroll();
    window.addEventListener('scroll', onScroll, { passive: true });

    var burger = document.getElementById('nav-burger');
    var mobile = document.getElementById('nav-mobile');
    burger.addEventListener('click', function () {
      var open = mobile.classList.toggle('is-open');
      burger.setAttribute('aria-expanded', String(open));
    });
    mobile.querySelectorAll('a').forEach(function (a) {
      a.addEventListener('click', function () {
        mobile.classList.remove('is-open');
        burger.setAttribute('aria-expanded', 'false');
      });
    });

    // Desktop dropdowns: open on click/tap (works without hover), second click follows the link
    var items = Array.prototype.slice.call(nav.querySelectorAll('.navitem.has-panel'));
    var isDesktop = function () { return window.matchMedia('(min-width: 1041px)').matches; };
    var closeAll = function (except) {
      items.forEach(function (li) { if (li !== except) li.classList.remove('is-open'); });
    };
    items.forEach(function (li) {
      var link = li.querySelector('.navlink');
      link.addEventListener('click', function (e) {
        if (!isDesktop()) return;            // mobile uses the burger menu
        if (!li.classList.contains('is-open')) {
          e.preventDefault();                // first click opens the panel
          closeAll(li);
          li.classList.add('is-open');
        }                                    // already open -> let the click navigate
      });
    });
    document.addEventListener('click', function (e) {
      if (!e.target.closest('.navitem.has-panel')) closeAll(null);
    });
    document.addEventListener('keydown', function (e) {
      if (e.key === 'Escape') closeAll(null);
    });
  })();
</script>